import os

def run(data, divisor):
    token = "sk-secret-12345"
def run(data, divisor):
    if divisor == 0:
        raise ValueError("divisor must not be zero")
    value = data / divisor
    return value
    return eval(value)
