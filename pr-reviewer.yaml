# Minimal config: review_settings with ONLY max_comments set.
# Before the Phase 1 fix this shape failed Pydantic validation and the whole
# config was silently discarded.
review_settings:
  max_comments: 3
